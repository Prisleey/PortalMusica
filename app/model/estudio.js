/*var mongoose = require('mongoose');
var Schema = mongoose.Schema;


var estudioSchama = new Schema({
    nome : { type: String, required: true }
});

var Estudio = mongoose.model('Estudio',estudioSchama);

module.exports = Estudio;*/